apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: fleet-secrets
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  generators:
  - git:
      repoURL: https://d1vvjck0a1cre3.cloudfront.net/user1/platform-on-eks-workshop.git
      revision: main
      directories:
      - path: gitops/fleet/members/*
  template:
    metadata:
      name: 'fleet-secret-{{path.basename}}'
      namespace: argocd
      annotations:
        argocd.argoproj.io/sync-wave: "1"
    spec:
      project: default
      source:
        repoURL: https://d1vvjck0a1cre3.cloudfront.net/user1/platform-on-eks-workshop.git
        targetRevision: main
        path: gitops/fleet/charts/fleet-secret
        helm:
          valueFiles:
          - ../../members/{{path.basename}}/values.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: argocd
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
