# Remove password generator - use AWS Secrets Manager instead
# apiVersion: generators.external-secrets.io/v1alpha1
# kind: Password
# metadata:
#   name: keycloak
#   namespace: keycloak
# spec:
#   length: 36
#   digits: 5
#   symbols: 5
#   symbolCharacters: "/-+"
#   noUpper: false
#   allowRepeat: true
---
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: argocd-store
  namespace: keycloak
spec:
  provider:
    kubernetes:
      remoteNamespace: argocd
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          namespace: keycloak
          key: ca.crt
      auth:
        serviceAccount:
          name: keycloak-config
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: keycloak-config
  namespace: keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  refreshInterval: "30s"
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: keycloak-config
    creationPolicy: Owner
    deletionPolicy: Retain
    refreshPolicy: OnChange
    template:
      engineVersion: v2
      mergePolicy: Replace
      data:
        KC_BOOTSTRAP_ADMIN_PASSWORD: "{{ `{{.KEYCLOAK_ADMIN_PASSWORD}}` }}"
        KC_DB_USERNAME: keycloak
        KC_DB_PASSWORD: "{{ `{{.KEYCLOAK_DB_PASSWORD}}` }}"
        POSTGRES_DB: keycloak
        POSTGRES_USER: keycloak
        POSTGRES_PASSWORD: "{{ `{{.KEYCLOAK_DB_PASSWORD}}` }}"
        USER_PASSWORD: "{{ `{{.KEYCLOAK_USER_PASSWORD}}` }}"
  dataFrom:
  - sourceRef:
      generatorRef:
        apiVersion: generators.external-secrets.io/v1alpha1
        kind: Password
        name: "postgres-keycloak-password"
    rewrite:
    - transform:
        template: "KEYCLOAK_DB_PASSWORD"
  - sourceRef:
      generatorRef:
        apiVersion: generators.external-secrets.io/v1alpha1
        kind: Password
        name: "keycloak-admin-password"
    rewrite:
    - transform:
        template: "KEYCLOAK_ADMIN_PASSWORD"
  data:
    - secretKey: KEYCLOAK_USER_PASSWORD
      remoteRef:
        key: {{ .Values.global.resourcePrefix | default "peeks" }}-{{ .Values.aws.cluster_name}}/secrets
        property: user_password
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: postgres-keycloak-password
  namespace: keycloak
spec:
  length: 32
  digits: 5
  symbols: 5
  symbolCharacters: "/-+"
  noUpper: false
  allowRepeat: true
---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: keycloak-admin-password
  namespace: keycloak
spec:
  length: 32
  digits: 5
  symbols: 5
  symbolCharacters: "/-+"
  noUpper: false
  allowRepeat: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso-store
  namespace: keycloak
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: keycloak
  name: eso-store
rules:
  - apiGroups: [""]
    resources:
      - secrets
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - authorization.k8s.io
    resources:
      - selfsubjectrulesreviews
    verbs:
      - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: eso-store
  namespace: keycloak
subjects:
  - kind: ServiceAccount
    name: eso-store
    namespace: keycloak
roleRef:
  kind: Role
  name: eso-store
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: keycloak
spec:
  provider:
    kubernetes:
      remoteNamespace: keycloak
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          namespace: keycloak
          key: ca.crt
      auth:
        serviceAccount:
          name: eso-store
          namespace: keycloak
---
# AWS Secrets Manager ClusterSecretStore for platform secrets
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: aws-secrets-manager
spec:
  provider:
    aws:
      service: SecretsManager
      region: '{{.Values.aws.region}}'
